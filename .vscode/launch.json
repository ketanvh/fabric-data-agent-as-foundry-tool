{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Foundry + Fabric demo",
      "type": "debugpy",
      "request": "launch",
      "module": "foundry_fabric_demo",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}